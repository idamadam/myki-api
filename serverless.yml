service: myki-api

provider:
  name: aws
  project: myki-api
  region: ap-southeast-2
  runtime: nodejs8.10
  stage: dev

functions:
  getBalance:
    handler: handler.getBalance
    events:
        - http: 
            path: myki/balance
            method: post

